# noado.kr — 프로젝트 규칙

**그룹 공통 규칙(`~/antigravity/CLAUDE.md`)이 자동 상속된다.**
여기에는 그룹에 없는 **blog 고유 규칙만** 남긴다. 중복은 적지 않는다.

**현재 지점과 다음 할 일은 `docs/HANDOFF.md` 가 정본이다.**

---

## 이 프로젝트

**noado.kr** — 한국 노인 복지 정보 블로그. 목표는 **구글 애드센스 재승인**이다.
거절 사유는 "가치 없는 콘텐츠(low value content)".

- Astro 정적 사이트 → Vercel 배포. GitHub `knot4844/noado`.
- `git push origin main` 이 곧 배포다. **푸시 시각이 곧 게시 시각이다.**
- `openclaw/apps/autoadsense/` 는 7월에 버려진 프로토타입이다. 배포와 무관하다.

---

## 절대 하지 말 것 (blog 고유)

### 1. 조사 없이 글을 찍어내지 않는다

거절의 근본 원인이 **98편을 사흘(2026-07-09~11)에 몰아 발행한 것**이다.
구글 스팸 정책의 '대규모 콘텐츠 남용'에 해당한다.

**발행 속도: 하루 3편** (형 지시). 단 편수는 목표가 아니라 **상한**이다.
아래를 모두 못 채운 글은 그날 발행하지 않는다. 두 편만 나오는 날은 두 편만 낸다.

| 발행 전 필수 조건 | |
|---|---|
| 1차 출처 검증 | 법령·고시·공단 자료를 직접 확인했는가 |
| 커버리지 확인 | 기존 글과 중복되지 않는가 (`grep` 으로 전수 확인) |
| 고유 각도 | 이 사이트에만 있는 관점·경험이 있는가 |
| 구조 | 목차·표·FAQ·출처가 갖춰졌는가 |
| 접근성 | 명암비 4.5:1 이상, 모바일 가로 스크롤 없음 |

**편수를 채우려고 대충 쓰는 것이 사흘에 98편과 같은 짓이다.**
심사 결과가 나올 때까지 상한을 올리지 않는다. 올리게 되면 형에게 리스크를
말씀드리고 오케스트레이터에게도 알린다.

### 2. 발행일(`date:`)을 조작하지 않는다

매일 올린 것처럼 날짜를 흩어놓지 않는다. 구글은 첫 크롤 날짜를 갖고 있어
어긋나면 지금보다 더 나쁜 신호가 된다. 대신 **실제로 고친 날** `updated:` 를
정직하게 찍는다.

### 3. 보호자 노트는 AI 생성 금지 구역

`.caregiver-note` 블록에는 `docs/caregiver-facts.md` 에 **운영자 본인이
확인한 사실만** 들어간다. 지어낸 한 줄이 사이트 전체의 신뢰를 깎는다.
기억이 흐린 항목은 비워둔다. **없으면 노트를 넣지 않는다.**

서술 원칙: 실금·실변처럼 드러내기 어려운 것도 **감추지 않는다.** 숨기면
고증이 안 되고 같은 일을 겪는 가족에게 도움이 되지 않는다. 다만 자극적으로
소비하거나 연민을 구걸하지 않는다. 담담하게 적고 그것이 제도의 어느 지점을
드러내는지로 연결한다.

### 4. 승인 전까지 광고를 켜지 않는다

환경변수 `PUBLIC_ADS_ENABLED` 를 설정하지 않는다. 없어야 광고 슬롯이
렌더링되지 않는다. 심사용 `<head>` 게시자 스크립트는 그대로 둔다.

### 5. 법령·제도는 반드시 팩트체크 절차를 거친다. 예외 없다

**연금·의료·복지를 다루는 YMYL 사이트다.** 틀린 숫자 하나가 임종을 앞둔
가족이나 생계가 걸린 어르신의 판단을 그르친다. 애드센스 심사보다 이쪽이
더 중요하다.

1. **1차 출처를 직접 확인한다.** 법제처·찾기쉬운 생활법령정보, 보건복지부
   고시, 공단 공식 안내, 지자체 공고.
   **개인 블로그·언론 기사·AI 기억은 출처가 아니다.**
2. **조문명·고시명을 글에 명시한다.** 독자가 직접 확인할 수 있어야 한다.
3. **금액·기간·비율·기한은 연도를 함께 적는다.** 매년 바뀐다.
4. **검증 못 한 수치는 넣지 않는다.** 뭉개지 말고 뺀 뒤 확인처를 안내한다.
5. **공식 출처끼리 엇갈리면 직접 열어 확인한다.** 그래도 안 되면 그 수치를
   뺀다. 2차 자료뿐이면 `⚠️ 미검증` 으로 남겨두고 발행을 미룬다.
6. **글 하단에 안내 문구를 넣는다.** 제도는 개정되며 개별 사정에 따라
   달라지므로 관할 기관에서 최종 확인하라는 안내.

**의심스러우면 쓰지 않는다.** 편수보다 정확도가 우선이다.

### 6. 특정 병원·시설·의료진의 실명을 쓰지 않는다

운영자가 알려주더라도 쓰지 않는다. 특정 기관의 문제가 아니라 구조적 문제이며,
이름을 적으면 초점이 흐려지고 해당 기관에 부당하다.

---

## 작업 관례

- 임종·사별처럼 민감한 글에는 프론트매터에 `hideCalculator: true` 를 넣어
  복지 계산기 자동 삽입을 막는다.
- 새 글·수정 후 `npm run build` 로 검증한다.
- 명암비는 라이트·다크 **양쪽에서 4.5:1 이상**. 어르신이 주 독자다.
  실제로 브라우저에서 측정해 확인한다.
- 예약 발행은 `scripts/publish-next.sh` (크론 09/12/15시). 새 글을 만들지
  않고 이미 검증해 커밋해 둔 글만 하나씩 푸시한다.

## 참고 문서

```
docs/HANDOFF.md              현재 지점과 다음 할 일 (정본)
docs/caregiver-facts.md      운영자 확인 사실. 보호자 노트의 유일한 근거
docs/topic-backlog-100.md    소재 100선. 99편 커버리지 빈칸 기준
docs/publishing-plan.md      발행 순번·진행 현황·10단계 작성 절차
docs/hospice-research.md     조사 메모 + 미검증 항목 표시
```
