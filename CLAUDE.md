# noado.kr — 프로젝트 규칙

**그룹 공통 규칙은 `~/antigravity/CLAUDE.md` 를 함께 읽는다.**
이 저장소는 `~/Documents/autoadsense-blog` 에 있어 antigravity 밖이므로
그룹 문서가 자동 상속되지 않는다. 세션을 시작하면 직접 열어볼 것.

**현재 지점과 다음 할 일은 `docs/HANDOFF.md` 가 정본이다.**

---

## 이 프로젝트

**noado.kr** — 한국 노인 복지 정보 블로그. 목표는 **구글 애드센스 재승인**이다.
거절 사유는 "가치 없는 콘텐츠(low value content)".

- Astro 정적 사이트 → Vercel 배포. GitHub `knot4844/noado`.
- `~/antigravity/openclaw/apps/autoadsense/` 는 7월에 버려진 프로토타입이다.
  배포와 무관하니 건드리지 않는다.

---

## 절대 하지 말 것

1. **조사 없이 글을 찍어내지 않는다.**
   거절의 근본 원인이 **98편을 사흘(2026-07-09~11)에 몰아 발행한 것**이다.
   구글 스팸 정책의 '대규모 콘텐츠 남용'에 해당한다.

   **발행 속도: 하루 3편** (2026-08-16 형 지시).
   단, 편수는 목표가 아니라 상한이다. **아래 조건을 모두 못 채운 글은
   그날 발행하지 않는다.** 두 편만 나오는 날은 두 편만 낸다.

   | 발행 전 필수 조건 | |
   |---|---|
   | 1차 출처 검증 | 법령·고시·공단 자료를 직접 확인했는가 |
   | 커버리지 확인 | 기존 글과 중복되지 않는가 (`grep` 으로 전수 확인) |
   | 고유 각도 | 이 사이트에만 있는 관점·경험이 있는가 |
   | 구조 | 목차·표·FAQ·출처가 갖춰졌는가 |
   | 접근성 | 명암비 4.5:1 이상, 모바일 가로 스크롤 없음 |

   **채우지 못하면 미룬다. 편수를 채우려고 대충 쓰는 것이 사흘에 98편과
   같은 짓이다.** 지금 문제는 글이 적어서가 아니라 원본성이 없어서다.

2. **발행일(`date:`)을 조작하지 않는다.**
   매일 올린 것처럼 날짜를 흩어놓는 것은 하지 않는다. 구글은 첫 크롤 날짜를
   갖고 있어 어긋나면 지금보다 더 나쁜 신호가 된다.
   대신 실제로 글을 고친 날 `updated:` 를 정직하게 찍는다.

3. **보호자 노트에 확인되지 않은 경험을 쓰지 않는다.**
   `.caregiver-note` 블록에는 `docs/caregiver-facts.md` 에 운영자 본인이
   확인한 사실만 들어간다. **AI 생성 금지 구역이다.**
   지어낸 한 줄이 사이트 전체의 신뢰를 깎는다. 기억이 흐린 항목은 비워둔다.

4. **승인 전까지 광고를 켜지 않는다.**
   환경변수 `PUBLIC_ADS_ENABLED` 를 설정하지 않는다. 없어야 광고 슬롯이
   렌더링되지 않는다. 심사용 `<head>` 게시자 스크립트는 그대로 둔다.

5. **법령·제도 내용은 반드시 팩트체크 절차를 거친다. 예외 없다.**

   **모든 글에 적용되는 필수 절차다.** 이 사이트는 연금·의료·복지를 다루는
   YMYL 사이트다. 틀린 숫자 하나가 임종을 앞둔 가족이나 생계가 걸린
   어르신의 판단을 그르친다. 애드센스 심사보다 이쪽이 더 중요하다.

   **발행 전 반드시 밟을 단계:**

   1. **1차 출처를 직접 확인한다.**
      법령(법제처·찾기쉬운 생활법령정보), 고시(보건복지부),
      공단 공식 안내(국민건강보험공단·국민연금공단), 지자체 공고.
      **개인 블로그·언론 기사·AI 기억은 출처가 아니다.**
   2. **조문명·고시명을 글에 명시한다.** 독자가 직접 확인할 수 있어야 한다.
   3. **금액·기간·비율·기한은 연도를 함께 적는다.** 매년 바뀐다.
   4. **검증 못 한 수치는 넣지 않는다.** "대략 ~정도"로 뭉개지 말고 뺀다.
      뺀 자리에는 확인처(공단 1577-1000 등)를 안내한다.
   5. **출처가 2차 자료뿐이면 발행을 미룬다.**
      `docs/hospice-research.md` 처럼 `⚠️ 미검증` 표시를 달아 남겨두고,
      1차 출처를 찾은 뒤에 쓴다.
   6. **글 하단에 안내 문구를 넣는다.** 제도는 개정되며 개별 사정에 따라
      판정이 달라지므로 최종 확인은 관할 기관에서 하라는 안내.

   **의심스러우면 쓰지 않는다.** 편수보다 정확도가 우선이다.

6. **특정 병원·시설·의료진의 실명을 쓰지 않는다.**

7. **23:00~07:00 에는 발행하지 않는다.** (전 프로젝트 공통 규칙)
   푸시가 곧 배포이므로 **푸시 시각이 곧 게시 시각**이다. 밤늦게 글을
   완성했다면 커밋만 해두고 아침에 푸시한다.

---

## 작업 관례

- 민감한 글(임종·사별 등)에는 프론트매터에 `hideCalculator: true` 를 넣어
  복지 계산기 자동 삽입을 막는다.
- 새 글·수정 후에는 `npm run build` 로 검증한다.
- 배포는 `git push origin main` 으로 자동 실행된다.
  (2026-08-16 Vercel↔GitHub 연동 복구함. 수동은 `npx vercel --prod`)
- 명암비는 라이트·다크 양쪽에서 **4.5:1 이상**을 지킨다.
  어르신이 주 독자다. 실제로 브라우저에서 측정해 확인한다.

## 참고 문서

```
docs/HANDOFF.md              현재 지점과 다음 할 일 (정본)
docs/caregiver-facts.md      운영자 확인 사실. 보호자 노트의 유일한 근거
docs/topic-backlog-100.md    소재 100선. 99편 커버리지 빈칸 기준
docs/hospice-research.md     호스피스 조사 메모 + 미검증 항목 표시
```
